#!/usr/bin/env python

def square_root(a):
    print "Square root of", a, "is:"
    y = 0
    result = 0
    while (a - result) > x:
        result = y*y
        y += 0.01
    answer = y-0.01 #we need the iteration before last
    print answer

x = 0.001

if __name__ == '__main__':
    q = raw_input('Find the square root of: ')
    square_root(float(q))


